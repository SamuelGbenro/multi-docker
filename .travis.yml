sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t stephengrider/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true stephengrider/react-test npm test

after_success:
  - docker build -t oyedelefavor/multi-client ./client
  - docker build -t oyedelefavor/multi-nginx ./nginx
  - docker build -t oyedelefavor/multi-server ./server
  - docker build -t oyedelefavor/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push oyedelefavor/multi-client
  - docker push oyedelefavor/multi-nginx
  - docker push oyedelefavor/multi-server
  - docker push oyedelefavor/multi-worker
